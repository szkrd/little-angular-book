import { LogDomDirective } from './log-dom.directive';

describe('LogDomDirective', () => {
  it('should create an instance', () => {
    const directive = new LogDomDirective();
    expect(directive).toBeTruthy();
  });
});
